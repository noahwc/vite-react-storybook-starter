# React + TypeScript + Vite + StorybookJS

A starter project.

Start dev server:
`npm run dev`

Build:
`npm run build`

Start storybook:
`npm run storybook`

Build static storybook:
`build-storybook`
